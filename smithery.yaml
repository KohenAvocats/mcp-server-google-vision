runtime: "container"
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
startCommand:
  type: "http"
  configSchema:
    type: object
    properties:
      googleApiKey:
        type: string
        description: "Your Google Cloud Vision API key"
    required:
      - googleApiKey
